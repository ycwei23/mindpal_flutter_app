import 'package:flutter/material.dart';

class AssistantMessageCard extends StatelessWidget {
  const AssistantMessageCard({super.key});

  @override
  Widget build(BuildContext context) {
    return Card();
  }
}