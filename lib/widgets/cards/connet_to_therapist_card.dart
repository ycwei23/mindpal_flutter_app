import 'package:flutter/material.dart';

class ConnectToTherapistCard extends StatelessWidget {
  const ConnectToTherapistCard({super.key});

  @override
  Widget build(BuildContext context) {
    return Card();
  }
}